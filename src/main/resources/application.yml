micronaut:
    application:
        name: kcui
    http.client:
        read-timeout: 1m
    router:
        static-resources:
            default:
                paths: classpath:public
                mapping: /**
                enabled: true

netty:
    default:
        allocator:
            max-order: 3